# Task 1 - CI Pipeline

A simple GitHub Actions pipeline that runs automatically on pushes and pull requests.

## What it checks

- installs the project dependencies
- checks the Python code with flake8
- runs the test with pytest

## Why

The aim is to catch problems automatically before changes move further through the delivery process.
